version: '3.8'

services:

  app:
    image: ubuntu:18.04
    environment: 
      PORT: 3001
    working_dir: /app
    volumes:
      - ./dist:/app
    ports:
      - "3001:3001"
    command: /app/server
    